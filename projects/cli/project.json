{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "cli",
  "projectType": "library",
  "sourceRoot": "projects/cli/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p projects/cli/tsconfig.lib.json && node projects/cli/scripts/prepare-package.mjs",
        "cwd": "."
      },
      "outputs": [
        "{workspaceRoot}/dist/cli"
      ]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config projects/cli/vitest.config.ts",
        "cwd": "."
      }
    }
  },
  "tags": []
}
