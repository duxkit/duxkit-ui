import { Conversation } from './conversation';

describe('Conversation', () => {
  it('should create an instance', () => {
    const directive = new Conversation();
    expect(directive).toBeTruthy();
  });
});
