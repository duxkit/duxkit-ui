import { ConversationContent } from './conversation-content';

describe('ConversationContent', () => {
  it('should create an instance', () => {
    const directive = new ConversationContent();
    expect(directive).toBeTruthy();
  });
});
