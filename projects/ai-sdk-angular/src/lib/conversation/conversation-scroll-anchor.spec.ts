import { ConversationScrollAnchor } from './conversation-scroll-anchor';

describe('ConversationScrollAnchor', () => {
  it('should create an instance', () => {
    const directive = new ConversationScrollAnchor();
    expect(directive).toBeTruthy();
  });
});
