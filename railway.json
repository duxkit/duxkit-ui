{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "pnpm build:www"
  },
  "deploy": {
    "startCommand": "sh -c 'pnpm exec nx run www:serve-static --host=0.0.0.0 --port=${PORT:-8080} --watch=false'",
    "healthcheckPath": "/"
  }
}
